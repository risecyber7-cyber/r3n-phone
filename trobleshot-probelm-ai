I need you to troubleshoot my ARM64 QEMU r3n-phone boot setup carefully. DO NOT assume files exist just because a previous step said they should. Base your diagnosis strictly on the outputs below.

SYSTEM:
- Host: Linux x86_64
- Project: /home/zenr3n/r3n-phone
- Target architecture: ARM64 / AArch64
- Kernel: 6.12.101+deb13-arm64
- QEMU: qemu-system-aarch64
- Root filesystem: images/rootfs.img
- rootfs.img size: 8.0G
- rootfs filesystem: ext4
- rootfs was successfully mounted at /mnt using /dev/loop0

IMPORTANT ROOTFS VERIFICATION:
file /mnt/lib/systemd/systemd
=> ELF 64-bit LSB pie executable, ARM aarch64, dynamically linked

file /mnt/usr/bin/dash
=> ELF 64-bit LSB pie executable, ARM aarch64

file /mnt/usr/lib/aarch64-linux-gnu/ld-linux-aarch64.so.1
=> ELF 64-bit LSB shared object, ARM aarch64

ls -l /mnt/lib/ld-linux-aarch64.so.1
=> symlink to aarch64-linux-gnu/ld-linux-aarch64.so.1

Therefore the actual rootfs appears to be a valid ARM64 Debian filesystem.

INITIAL QEMU BOOT:
I booted:

qemu-system-aarch64 \
  -machine virt \
  -cpu cortex-a76 \
  -smp 4 \
  -m 4096 \
  -kernel kernel/vmlinuz-6.12.101+deb13-arm64 \
  -initrd kernel/initrd.img \
  -append "root=/dev/vda rw console=ttyAMA0" \
  -drive file=images/rootfs.img,format=raw,if=virtio \
  -nographic

The ARM64 kernel itself boots successfully.

Important kernel messages:

"virtio-pci ... enabling device"
so the virtio device is detected.

Then:

"Run /init as init process"

But:

"Failed to execute /init (error -8)"

Then:

"Run /sbin/init as init process"

and:

"Starting init: /sbin/init exists but couldn't execute it (error -8)"

Then:

"Starting init: /bin/sh exists but couldn't execute it (error -8)"

Finally:

"Kernel panic - not syncing: No working init found."

The repeated important error was:

"request_module: modprobe binfmt-464c cannot be processed, kmod busy with 50 threads..."

This happened repeatedly before the init failure.

The original initramfs was 105M and came from Debian's generated initramfs. It contained lots of x86 microcode:

kernel/x86/microcode/AuthenticAMD.bin

while also containing ARM64 modules.

I then attempted to create a minimal custom ARM64 initramfs.

CURRENT PROBLEM:
I created /tmp/r3n-initrd and tried to create:

/home/zenr3n/r3n-phone/kernel/initrd-r3n-arm64.img

However the resulting archive is only:

-rw-rw-r-- ... 551 bytes

This is clearly suspicious.

The archive contains:

init
bin/busybox

but `bin/busybox` is ONLY a symlink:

bin/busybox -> ../usr/bin/busybox

and the actual file:

/tmp/r3n-initrd/usr/bin/busybox

DOES NOT EXIST.

I verified:

file /tmp/r3n-initrd/usr/bin/busybox
=> cannot open ... No such file or directory

The archive was created with:

cd /tmp/r3n-initrd
sudo find . -print0 | sudo cpio --null -o -H newc | gzip -9 > ~/r3n-phone/kernel/initrd-r3n-arm64.img

and reported:

3 blocks

When extracted, it contained:

init
bin/busybox -> ../usr/bin/busybox

but no actual BusyBox binary.

I also mistakenly tried:

/boot/initrd.img-6.12.101+deb13-arm64

which produced "Permission denied" because an initramfs archive is not an executable.

Then I tried:

zcat /boot/initrd.img-6.12.101+deb13-arm64 | cpio -idm

but got:

gzip: ... not in gzip format

because `file` showed:

/boot/initrd.img-6.12.101+deb13-arm64:
ASCII cpio archive (SVR4 with no CRC)

So the Debian initramfs is an uncompressed CPIO archive in this environment, not gzip.

I also accidentally ran QEMU from /tmp/check-new-initrd and got:

Could not open 'images/rootfs.img': No such file or directory

This is just a relative-path/current-directory problem. The real rootfs does exist:

/home/zenr3n/r3n-phone/images/rootfs.img

and:

ls -lh /home/zenr3n/r3n-phone/images/rootfs.img
=> 8.0G

The kernel also exists:

/home/zenr3n/r3n-phone/kernel/vmlinuz-6.12.101+deb13-arm64
=> 36M

WHAT I NEED FROM YOU:

1. Identify the REAL cause of the original `error -8`.
2. Explain whether `error -8` is consistent with an architecture/ELF format problem.
3. Explain the significance of the repeated `binfmt-464c` modprobe messages.
4. Determine whether the original Debian initramfs is accidentally trying to execute x86_64 binaries on the ARM64 kernel.
5. DO NOT tell me to continue building an initramfs until we have verified the BusyBox binary actually exists.
6. Give me commands to locate a real ARM64 BusyBox binary on my host/rootfs/initramfs.
7. If BusyBox is not installed, give me the safest Debian command to install an ARM64-compatible/static BusyBox for this initramfs.
8. The BusyBox binary must actually be AArch64 and preferably statically linked so the initramfs does not depend on external ARM64 libraries.
9. Show exactly how to copy the REAL BusyBox ELF binary into:

/tmp/r3n-initrd/bin/busybox

Do NOT create a symlink to a nonexistent `/usr/bin/busybox`.

10. Then create the required symlinks for BusyBox applets if needed, or explain whether they are necessary when the init script uses `/bin/busybox`.
11. Give me a correct `/tmp/r3n-initrd/init` script that:
    - is executable
    - uses the actual BusyBox binary
    - mounts /proc
    - mounts /sys
    - mounts /dev
    - waits for /dev/vda
    - mounts /dev/vda as ext4 at /newroot
    - performs switch_root
    - starts `/sbin/init` from the ARM64 rootfs

12. Before packing, give me commands that MUST prove:

file /tmp/r3n-initrd/bin/busybox

and the output must contain:

ELF ... ARM aarch64

and preferably:

statically linked

13. Give me commands to verify the final CPIO archive contains the ACTUAL binary, not just a symlink.

For example, I want a check that extracts `bin/busybox` and runs `file` on the extracted file.

14. Make sure the archive is actually gzip compressed if the QEMU/kernel invocation expects gzip, and explain how to verify it with:

file kernel/initrd-r3n-arm64.img

15. Give me the final QEMU command using ABSOLUTE PATHS so the current working directory cannot cause the rootfs "No such file or directory" problem.

Use:

/home/zenr3n/r3n-phone/images/rootfs.img

and:

/home/zenr3n/r3n-phone/kernel/vmlinuz-6.12.101+deb13-arm64

and:

/home/zenr3n/r3n-phone/kernel/initrd-r3n-arm64.img

16. IMPORTANT: Before giving me the final boot command, make me run the verification commands first. I will paste their output back to you.

17. Do not blindly repeat the previous instructions. There is already a proven mistake: the previous instructions created a 551-byte initramfs containing a broken BusyBox symlink with no actual BusyBox binary.

18. Also investigate whether `root=/dev/vda` is correct for `-drive ...,if=virtio` with `-machine virt`, and whether any required virtio/ext4 drivers need to be built into the kernel or included in the initramfs.

19. Explain one thing at a time and give me a small sequence of commands. Do not give me 50 commands at once.

CURRENT STATE:
- ARM64 rootfs: VALIDATED
- ARM64 kernel: BOOTS
- virtio block device: DETECTED
- rootfs image: EXISTS
- custom initramfs: BROKEN
- custom BusyBox: MISSING
- custom initramfs size: 551 bytes
- original failure: `Failed to execute /init (error -8)`
- final original failure: `No working init found`

Start by diagnosing the architecture/initramfs problem and give me ONLY the first verification commands needed to find a real ARM64 BusyBox binary. Do not proceed to packing or QEMU boot yet.
